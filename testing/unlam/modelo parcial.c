/* 
Ejercicio 1:

Se ingresan valores entero entre -50 y 50 inclusive, finalizando el ingreso
con el valor -100. Se debe validar el ingreso mediante una función para no permitir
un número fuera dle rango permitido.

Calcular e informar:

a. Cantidad de números negativos ingresados. Para determinar si un número es negativo
utilizar una función que retornara 1 si es negativo, 0 si no lo es.

b. Promedio de los números positivos. Para el cálculo del promedio utilizar 
una función. Que retornara el promedio si el contador es mayor a cero si no 
retornara 0.

c. Promedio de todos los números ingresados sin incluir el -100. Para el 
cálculo del promedio utilizar la misma función del punto anterior.

d. El menor valor ingresado y su orden de ingreso, solo para informar ambos
valores utilizar una funcion.

Ejercicio 2:

En un negocio trabajan 50 vendedores. Cada vez que se realiza uan venta
durante el día, se emite una factura donde se indican los siguiente
datos:

* Código de vendedor (entero de 1 a 50).
* Importe de la venta (flotante, mayor que cero).

Un vendedor puede realizar más de una venta en el día, una o ninguna.
La carga de datos en un vector finaliza con el ingreso de un código de 
vendedor igual cero.

Al finalizar la carga, se debe informar: 

a. Importe total de facturacion realizado por cada vendedor
b. El código de vendedor que vendió el mayor importe de facturación durante el día
c. Los códigos de los vendedores que no realizaron ventas.
d. Los códigos de los vendedores que superaron el promedio total de facturación.

 */